node_modules
dist
release
# Vendored Java LSP: source only; runtime jars are regenerated with Maven
lsp-servers/java-language-server/dist/
.DS_Store
*.log
# Temporary: release notes go into the tag message
release-notes.md
